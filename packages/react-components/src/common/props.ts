export enum EBgType {
  fill = 'fill',
  stroke = 'stroke',
}

export enum EColor {
  primary = 'primary',
  secondary = 'secondary',
  black = 'black',
  grey = 'grey',
  success = 'success',
  wrong = 'wrong',
  white = 'white',
  'light_grey' = 'light_grey',
}

export enum EAlign {
  left = 'left',
  center = 'center',
  right = 'right',
}

export enum ESize {
  small = 'small',
  medium = 'medium',
  large = 'large',
}
