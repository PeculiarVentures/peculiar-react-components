@each $key in $colors
	.stroke_$(key)
		&:not(svg)
			border-width: 1px
			border-style: solid
			border-color: rgb(var(--$(key)))
		svg& [data-stroke]
			stroke: rgb(var(--$(key)))

.stroke_dashed
	border-style: dashed
