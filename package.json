{
  "name": "react-components-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run --parallel clean",
    "lint": "lerna run --parallel lint",
    "test": "lerna run --parallel test",
    "dev:docs-app": "yarn --cwd packages/docs-app development",
    "bootstrap": "yarn",
    "bootstrap:undo": "lerna clean --yes"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "author": "PeculiarVentures Team",
  "license": "AGPL-3.0"
}
